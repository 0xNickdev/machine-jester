{
  "name": "machine-jester-backend",
  "version": "2.0.0",
  "description": "Machine Jester — OpenClaw Gateway",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "node --max-old-space-size=4096 setup-runtime.js && openclaw gateway --port $PORT"
    "setup": "node setup-runtime.js"
  },
  "dependencies": {
    "openclaw": "latest"
  }
}
